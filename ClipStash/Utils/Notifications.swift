import Foundation

/// Custom notifications for app communication
extension Notification.Name {
    /// Posted when global hotkey is pressed to toggle popover
    static let togglePopover = Notification.Name("ClipStash.togglePopover")
}
